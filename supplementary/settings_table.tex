  {\footnotesize
   \setlength{\tabcolsep}{4pt}
   \renewcommand{\arraystretch}{1.15}
   \begin{longtable}{p{2.4cm}p{2.6cm}p{2.4cm}>{\raggedright\arraybackslash}p{5.6cm}}
      \caption{Inputs, outputs, software and settings of each analysis.}
      \label{tab:settings} \\
      \toprule
      Analysis & Input & Output & Software and settings \\
      \midrule
      \endfirsthead
      \toprule
      Analysis & Input & Output & Software and settings \\
      \midrule
      \endhead
      Phase classification & filtered greyscale volume & phase map (air, ice, grain) & own code; $3^3$ median; valley thresholds per scan for beads, three-class Otsu for sand \\
      Bead labels & phase map & grain labels & own code; marker-controlled watershed on the distance transform; shape gate on fill, radius and roughness \\
      Sand labels & greyscale volume & grain labels & own 3D U-Net, border--core, trained on synthetic packings; selected against watershed, a uniform-intensity U-Net, ParticleSeg3D and Cellpose-SAM (Table~2 of the paper) \\
      Bead DDIC & greyscale scans $N$, $N{+}1$; labels of $N$ & per-grain $\Phi$; translation used & SPAM~0.9 \texttt{spam-ddic}; full affine, label dilation 2, margin 15 voxels, 300 iterations, two-level multiscale, updated gradient, per-grain seed; converged at return status~2, usable at~1; grey scales mapped on a copy where they differed \\
      Sand tracking & labels of both scans; column lengths & matched grain pairs & own code; affine predictor from column length, refinement over the 12 nearest accepted matches, half-spacing tolerance, volume ratio 0.6--1.6, reciprocity required \\
      Strain and rotation & accepted translations & $\mathbf{G}$, $\mathbf{E}$, $E_\mathrm{eq}$, rotation & own code; least squares with intercept over 12 (beads) or 16 (sand) nearest neighbours; Green--Lagrange; Eq.~(7) of the paper; composed $1\!\to\!N$ fields re-fitted at scan-1 positions; rigid column motion removed \\
      Bond census & phase map, labels, matched pairs & throats, $\bar z$, neck radius, survival, coverage pair, class & own code; lens footprint $0.40R$; neck on the plane through the narrowest free point; survival classes at 12 voxels; coverage in a 1--3 voxel shell on a $3\times$ upsampled grid; $C_\mathrm{HI} \in \{0.5,0.7,0.8,0.9\}$, reference 0.8; answerable at gap $\geq 7$ voxels \\
      Breakage clustering & broken-bond midpoints & cluster size, RMS, $p$ & scikit-learn DBSCAN, min.\ samples 5, clusters $\geq 6$; neighbourhood one grain diameter, sweep 0.6--2.0; 400 resamplings of the bonds present; Benjamini--Hochberg across specimens \\
      Core structure & phase map & $\varepsilon_\mathrm{c}$, $\phi_\mathrm{ice}$, $\rho$, $S_V$, spanning air, $k/k_0$ & own code; core radius $0.85$ of the 98th-percentile grain-cloud radius, height band 0.20--0.80 of the column, re-centred per scan; $S_V$ by voxel-face count; 26-connectivity on the $2\times$-binned map, spanning between the terminal 5\,\% bands; Carman form with $k_K = 5$ \\
      Crack classes & phase map, labels & void, surface and body crack, separation & own code; bead envelope (closing $1.25R$, holes filled, 4-voxel skin, one bead diameter clear of each platen); void ceiling \SI{2}{\milli\meter\cubed}; crack width $\leq$ \SI{0.25}{\milli\meter}; skin 20 voxels; bodies $< 100$ voxels dropped; change from the unloaded scan reported \\
      Ice tortuosity & ice phase of interior cubes & $D_{\mathrm{eff},z}$, $\tau_\mathrm{ice}$ & pumapy~3.2.2 \texttt{compute\_continuum\_tortuosity}; $2\times$-binned map; $128^3$ voxels (\SI{6.3}{\milli\meter}); three cubes at 0.28--0.72 of the column on the axis, $\geq 98\%$ inside the specimen; $D_0 = 1$ in ice, 0 elsewhere; $c = 0/1$ on the axial faces, periodic sides; conjugate gradient, tolerance $10^{-6}$, $2\times10^5$ iterations; accepted if $D_{\mathrm{eff},z} > 0$ and $\tau \geq 1$; mean over accepted cubes \\
      Ice-path map & phase map & detour of the shortest ice path from the support forced by the crack air & own code, scikit-image \texttt{MCP\_Geometric}, unit cost; grain envelope (per-slice closing $1.25R$, holes filled, 2-voxel skin; sand pulled in \SI{1}{\milli\meter}); crack air = air inside the envelope removed by a \SI{0.25}{\milli\meter} opening; largest 26-connected body of ice plus crack air, ice taken inside it; seeds on the first ice voxel of every axial line within \SI{3}{\milli\meter} of the support, the same for both paths; drawn where the path through the ice exceeds the path through the healed ice by more than \SI{0.3}{\milli\meter}, opened once, bodies $\geq$ \SI{0.5}{\milli\meter\cubed}; $2\times$-binned maps for beads, full resolution for sand \\
      \bottomrule
   \end{longtable}}